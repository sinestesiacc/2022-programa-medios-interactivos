void setup() {
  // formato 16:9
  // dimensiones medidas en pixeles
  // size(ancho, alto); 
  size(640, 360);
}

void draw() {
  //ellipse(mouseX, mouseY, 80, 80);
  
  rect(mouseX, mouseY, 50, 100);
}
